# バージョン5に向けて仕様決定

ライブラリとしての仕様について：

1. パターン末尾の半角空白
2. ゼロ長一致の取り扱い
3. 一致、空一致、不一致、構文エラー、エンジンエラーの結果の取り扱い
4. `pure elemental`
5. `^`と`$`
6. Unicodeスカラ値の範囲
7. 文字クラスの末尾と連続ハイフンについて

テストに関して：

1. `test`スイートの充実
2. FPMとCMAKEで同一テスト集合の登録

## パターン末尾のホワイトスペースの取り扱い

現状について：

- `is_valid_regex`, `.in.`, `regex`は`trim(pattern)`を使う（src/forgex.F90: L67, L95, L260）
- `.match.`は通常のパターン本体を保持して、末尾を`$`を処理する経路だけ`len_trim`を使う（src/forgex.F90: L182-190）

調査事項：`trim`関数／`adjustl`が除去する文字の集合に、ホワイトスペース以外に何が含まれるかを調査する。

決定：「**引数値を無加工で保持する**」

注意：

- 例として、V4.6までの固定長文字列への代入で`pattern='abc'`は、`abc`と末尾空白の列として解釈される。
- 固定長のパターンバッファをそのまま渡していた利用者は`text .in. trim(patten)`や`call regex(trim(pattern), text, ...)`のような修正が必要となる

記載事項：

- 「パターンはFortran文字列の全長をそのまま解釈する」
- 「先頭／末尾のホワイトスペースを削除しない」
- 「固定長変数の空白埋めを無視したい場合には、呼び出し側で`trim`する」
- 「ホワイトスペースを無視するモードは存在しない」
- `' '`, `'[ ]'`, 16進エスケープ文字は、いずれも空白を表現できるが、自動トリムは行わない」
- 破壊的変更：移行案内において「**`.in.`, `regex`, `is_valid_regex`での暗黙のトリムは廃止される**」と記す

テスト事項：

- 以下を全APIで比較する
  - `' '`, `'a '`, `' a'`, `'a b'`
  - 全て空白のパターン
  - ゼロ長パターン
  - 固定長変数を直接渡す場合とトリムして渡す場合
  - 末尾空白を含むリテラル最適化経路
  - 空白の後ろに`$`がある場合
- `is_valid_regex`は`.match.`APIと同じパターン値を構文検査するべき

## ゼロ長一致の取り扱い

現状について：

